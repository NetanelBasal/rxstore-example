import { PhotonDirective } from './photon.directive';

describe('PhotonDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotonDirective();
    expect(directive).toBeTruthy();
  });
});
